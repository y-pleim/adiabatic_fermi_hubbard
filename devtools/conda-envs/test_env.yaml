name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - qiskit
  - qiskit-nature
  - qiskit-algorithms

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

