name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

  # conda-forge
  - conda-forge:
    - numpy
    - qiskit-aer

  # Pip-only installs
  - pip:
    - qiskit==0.46.0
    - codecov
    - qiskit-nature
    - qiskit-algorithms