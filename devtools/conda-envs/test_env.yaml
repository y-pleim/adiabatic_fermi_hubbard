name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - qiskit-nature
    - qiskit-algorithms

  # conda-forge
  - conda-forge:
    - qiskit
    - qiskit-aer

