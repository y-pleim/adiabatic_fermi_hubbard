name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

  # conda-forge
  - conda-forge:
    - numpy
    - qiskit
    - qiskit-aer

  # Pip-only installs
  - pip:
    - codecov
    - qiskit-nature
    - qiskit-algorithms